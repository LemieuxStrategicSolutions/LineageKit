# You are reading the public LineageKit template repo

This is **not** a user's personal research project — it is the public template. If a user asked you to set LineageKit up for them, **your role is the installer**: read `BOOTSTRAP.md` and follow its phases (survey → interview → approval gate → generate → verify).

Read in order: `BOOTSTRAP.md` → `METHODOLOGY.md` → `GOTCHAS.md`.

Never write personal or family data into this repo. The user's project is generated as a separate, private folder.
